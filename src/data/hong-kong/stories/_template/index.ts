/**
 * Story Template Entry Point
 * 
 * 导出故事配置和场景数据
 * 不要修改此文件的结构
 */

import { storyConfig } from './story.config';
import { scenarioData } from './scenario.data';

export default {
  config: storyConfig,
  scenarios: scenarioData,
};
